<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MapsXF.PointView"
             xmlns:local="clr-namespace:MapsXF"
             xmlns:controls="clr-namespace:MapsXF.Controls"
             Visual="Material">

    <Frame Margin="8" VerticalOptions="End">
        <ScrollView>
            <StackLayout>
                <!--<Label Text="{Binding InfoText}" />-->
                <Button Text="Navigate" Command="{Binding NavigateCommand}" />
                <Button Text="Gen Close" Command="{Binding CloseCommand}" />
            </StackLayout>
        </ScrollView>
    </Frame>
</ContentView>